# Local .terraform directories
**/.terraform/*
.terraform.lock.hcl

# Terraform state files
*.tfstate
*.tfstate.*

# Crash logs
crash.log

# Terraform variable files (that might have secrets)
*.tfvars
*.tfvars.json

# Override files (used to customize Terraform locally)
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Terraform provider plugins (if manually installed)
.terraformrc
terraform.rc
